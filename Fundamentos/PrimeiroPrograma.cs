using System;
using System.Collections.Generic;
using System.Text;

namespace CursoCSharp.Fundamentos {
    class PrimeiroPrograma {
        static void Main() { 
            /* Azul: Palavras reservadas da linguagem, Cinza: Indica que a palavra digitada não pe necessária. 
             * Uso do System no using 
             * Main: Método que é porta de entrada de programa CSharp. 
             * Problemas múltiplos método Main definido. 1° Solução: Alterar o nome do método toda vez que criar um novo.  
             * 2° Solução: Botão direito mouse -> propriedades -> aplicativos -> objetos de inicialização -> definir o método que pretende executar.
             * 
             */
            System.Console.Write("Primeiro");
            Console.WriteLine("Programa");
            Console.WriteLine("Terminou");
        }
    }
}
